@if (Table != null)
{
    @if (Table.Rows > 1)
    {
        <TableView3D Table="Table" TableChanged="TableChanged" />
    }
    else
    {
        <TableView2D Table="Table" TableChanged="TableChanged" />
    }
}
else
{
    <div class="alert alert-info">Select a table from the list above to view its data.</div>
}

@code {
    [Parameter]
    public TableData? Table { get; set; }

    [Parameter]
    public EventCallback<TableData> TableChanged { get; set; }
}